# ====================================
# QIIC Backend - Production Environment
# Server: Hostinger VPS (194.164.72.37)
# ====================================

# Server Configuration
NODE_ENV=production
PORT=4000

# Database - Hostinger MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=u262632985_qic
DB_USER=u262632985_qiic
DB_PASSWORD=Bon00@@71bon

# JWT Secret (⚠️ مهم: استخدم مفتاح فريد للإنتاج - لا تشارك هذا المفتاح)
# يمكنك توليد مفتاح جديد: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=f8e7d6c5b4a39281706f5e4d3c2b1a0908d7c6b5a493827160f5e4d3c2b1a098

# CORS Origins (الدومين الفعلي + IP للاختبار)
CORS_ORIGIN=https://ielts.sbs,https://www.ielts.sbs,http://194.164.72.37,http://ielts.sbs

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# Admin Configuration (اختياري - إذا كان لديك admin backend)
# ADMIN_EMAIL=admin@qic.com.qa
# ADMIN_PASSWORD_HASH=<bcrypt_hash_here>

# Logging
LOG_LEVEL=info
LOG_FILE=/var/log/qiic/app.log

# Session (إذا كنت تستخدم sessions)
# SESSION_SECRET=YOUR_SESSION_SECRET_HERE_MIN_32_CHARS_RANDOM

# SSL/TLS (سيتم التعامل معها عبر Nginx)
# SSL_ENABLED=false

# Socket.IO Configuration
SOCKET_PATH=/socket.io
SOCKET_PING_TIMEOUT=60000
SOCKET_PING_INTERVAL=25000

# ====================================
# ⚠️ ملاحظات مهمة:
# 1. لا ترفع هذا الملف إلى Git (موجود في .gitignore)
# 2. انسخ هذا الملف يدوياً إلى السيرفر عبر SSH
# 3. غيّر القيم الحساسة قبل الرفع
# ====================================
